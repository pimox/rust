Index: rustls-native-certs/Cargo.toml
===================================================================
--- rustls-native-certs.orig/Cargo.toml
+++ rustls-native-certs/Cargo.toml
@@ -40,7 +40,5 @@ version = "0"
 default = ["rustls"]
 [target."cfg(all(unix, not(target_os = \"macos\")))".dependencies.openssl-probe]
 version = "0.1.2"
-[target."cfg(target_os = \"macos\")".dependencies.security-framework]
-version = "2.0.0"
 [target."cfg(windows)".dependencies.schannel]
 version = "0.1.15"
